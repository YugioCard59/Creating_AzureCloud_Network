#Commands to list all local containers, start selected container and attach selected container to the jump_box.  All commands run as jump_box user until user becomes ansible container.

$ sudo docker ps -as
#note name of ansible container that is needed
$ sudo docker start <insert container name>
$ sudo docker attach <insert container name>
